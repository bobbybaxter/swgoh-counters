// const admin = require('../../../setup/firebase');

module.exports = async function updateUser(req, res) {
  // const {
  //   allyCode,
  // } = req.body;

  // const user = await admin.database().set({
  //   allyCode,
  // });

  // return res.send(user);
  return 'updateUser response';
};
